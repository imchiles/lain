<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//Mybatis.org//DTD Mapper 3.0//EN" 
	"http://Mybatis.org/dtd/Mybatis-3-mapper.dtd">
<mapper namespace="com.lain.dao.LocationMapper">
<!-- 这里还没有做修改。 -->
	<select id="findLocationAddress" resultType="int">
		select distinct elm_address from en_location_manage where di_id=#{diId}
	</select>
	
	<update id="updLocationStatusAndLen">
		update en_location_manage set elm_status=#{elm_status},elm_length=#{elm_length} where elm_address=#{elm_address} and di_id=#{di_id}
	</update>
	
	<select id="getLocation" resultType="com.lain.entity.LocationManagePojo">
		select * from en_location_manage  where elm_address=#{elm_address} and di_id=#{di_id}
	</select>
	
	<select id="selectLocationAll" resultType="com.lain.entity.LocationManagePojo">
		select * from en_location_manage  where elm_name = "定位漏水"
	</select>
</mapper>
